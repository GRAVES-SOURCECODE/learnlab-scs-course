title: LearnLab SCS
description: Learning lab course repo
template:
  repo: learnlab-scs-template
  name: learnlab-scs
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
